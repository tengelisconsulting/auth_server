[
 {kernel,
  [{logger_level, all},
   {logger,
    [{handler, default, logger_std_h,
      #{ level => info }}
    ]}]},

  {auth_server,
   [
    {runtime_args, #{
                    listen_port => 8888
                   }}
   ]}

].
