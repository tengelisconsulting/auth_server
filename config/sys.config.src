[
 {kernel,
  [{logger_level, all},
   {logger,
    [{handler, default, logger_std_h,
      #{ level => info }}
    ]}]},

  {auth_server,
   [
    {runtime_args, #{
                     session_s => 3600,
                     priv_key_file => "/app/priv_key",
                     listen_port => "${AUTH_HTTP_PORT}",
                     db_api_port => "${API_DB_PORT}",
                     db_api_host => "${API_DB_HOST}"
                    }}
   ]}

].
